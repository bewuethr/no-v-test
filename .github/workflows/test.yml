name: Test permissions

on:
  workflow_dispatch:


permissions:
  contents: none

jobs:
  job-1:
    name: Job 1
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
